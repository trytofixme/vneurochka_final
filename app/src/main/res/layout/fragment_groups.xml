<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".view.fragments.GroupsFragment">

    <RelativeLayout
        android:id="@+id/relative_layout_chat_fragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="40dp">
    <EditText
        android:id="@+id/et_group_search"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginStart="15dp"
        android:autofillHints=""
        android:maxLines="1"
        android:layout_marginTop="5dp"
        android:layout_marginEnd="15dp"
        android:background="@drawable/background_edit_text_search"
        android:hint="@string/search"
        android:inputType="text"
        android:paddingStart="15dp"
        android:focusable="true"
        android:paddingEnd="10dp"
        android:textColor="@color/black"
        android:textColorHint="#797B7E"
        android:textSize="15sp"
        android:drawableEnd="@drawable/ic_baseline_search_24"/>

        <ImageView
            android:id="@+id/iv_group_fragment_empty"
            android:layout_width="match_parent"
            android:layout_height="330dp"
            android:layout_centerInParent="true"
            android:layout_gravity="center"
            android:layout_marginStart="15dp"
            android:layout_marginTop="20dp"
            android:layout_marginEnd="20dp"
            android:src="@drawable/ic_empty_chat_view"
            android:visibility="gone"
            android:contentDescription="description" />
    </RelativeLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/groups_list_recycle_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="10dp"/>

</RelativeLayout>
